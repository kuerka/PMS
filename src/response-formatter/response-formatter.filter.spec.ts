import { ResponseFormatterFilter } from './response-formatter.filter';

describe('ResponseFormatterFilter', () => {
  it('should be defined', () => {
    expect(new ResponseFormatterFilter()).toBeDefined();
  });
});
